export type ProjectBasicInformationProps = {
  title: string;
  description: string;
  isDarkModeEnabled: boolean;
  isSearchEnabled: boolean;
};
export type ClientBasicInformationProps = {
  client: string;
  clientURL: string;
};
